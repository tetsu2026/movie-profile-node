# GitHub Issue アウトライン

## フェーズ構成

### Phase 0: Walking Skeleton（#1〜#6）
最小限の環境構築とエンドツーエンド動作確認。インフラ・認証の骨組みを完成させ、デプロイまでの自動化を実現。

### Phase 1: MVP Core（#7〜#14）
ユーザー価値を最速で届けるコア機能。プロフィール作成・公開ページ表示・動画アップロード機能を実装。

### Phase 2: 動画処理（#15〜#18）
動画エンコード処理の実装。Phase 1は同期処理で最小実装、リトライ・エラーハンドリングを強化。

### Phase 3: 管理・運用（#19〜#22）
管理者機能と運用に必要な機能。ユーザー管理・削除・監視機能を実装。

### Phase 4: 堅牢化（#23〜#26）
テスト・セキュリティ強化・パフォーマンス最適化。本番運用に向けた品質向上。

### Phase 5: 動画表示（#27）
公開プロフィールページでの動画再生機能を実装。

> **備考**: Phase 5は#17（動画エンコード機能）完了後に着手することを推奨。MVPのコア機能として、Phase 2完了後に実装するのが現実的。

### Phase 6: DB統一（#28）
Laravel版が PostgreSQL 単一 DB 構成に統合したのに合わせ、Node.js 版も MySQL → PostgreSQL に移行して設計書・運用環境を統一する。

---

## 依存関係マップ

```
Phase 0: 環境構築・認証
#1 → #2 → #3 → #4 → #5 → #6

Phase 1: MVPコア機能
#6 → #7 → #8 → #9
#7 → #10
#8 → #11
#9 → #12 → #13 → #14

Phase 2: 動画処理
#8 → #15 → #16 → #17 → #18

Phase 3: 管理・運用
#6 → #19 → #20 → #21
#1 → #22

Phase 4: 堅牢化
#14, #18 → #23
#23 → #24 → #25 → #26

Phase 5: 動画表示
#10, #14 → #27

Phase 6: DB統一
#3 → #28
```

---

## Issue アウトライン表

### Phase 0: Walking Skeleton

#### Issue #1: ローカル開発環境構築（Docker）

**概要**: Docker ComposeでLaravel + MySQL + MinIO（S3エミュレータ）のローカル開発環境を構築

**依存**: なし

**タスク領域**: infra

**受け入れ基準(AC)**:
- [ ] docker-compose.ymlが作成され、web/app/db/minioコンテナが起動する
- [ ] `docker-compose up -d`でLaravelが http://localhost で表示される
- [ ] MySQLコンテナに接続でき、マイグレーション実行が可能
- [ ] MinIOコンテナが起動し、S3互換のエンドポイントが利用可能
- [ ] .env.exampleと.envが適切に設定されている

---

#### Issue #2: Laravel 11.x 初期セットアップ

**概要**: Laravelプロジェクト作成、Laravel Breezeインストール、基本設定を完了

**依存**: #1

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] Laravel 11.xプロジェクトが作成され、composer installが完了
- [ ] Laravel Breeze（Blade + Tailwind CSS）がインストールされている
- [ ] `php artisan serve`でLaravelウェルカムページが表示される
- [ ] `npm install && npm run dev`でTailwind CSSがビルドされる
- [ ] .envファイルでDB接続設定が完了し、`php artisan migrate`が成功

---

#### Issue #3: データベーススキーマ作成（マイグレーション）

**概要**: users/profiles/videosテーブルのマイグレーションファイル作成と実行

**依存**: #2

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] usersテーブルのマイグレーションが作成され、role/email_verified_at/deleted_atカラムが含まれる
- [ ] profilesテーブルのマイグレーションが作成され、外部キー制約が正しく設定される
- [ ] videosテーブルのマイグレーションが作成され、status ENUM/retry_countカラムが含まれる
- [ ] `php artisan migrate`でエラーなく全テーブルが作成される
- [ ] 外部キー制約（CASCADE/SET NULL）が設計書通りに動作する

---

#### Issue #4: Eloquentモデル作成とリレーション定義

**概要**: User/Profile/Videoモデルの作成、リレーション定義、$fillable設定

**依存**: #3

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] User/Profile/Videoモデルが作成され、適切な名前空間に配置される
- [ ] User hasOne Profile、User hasMany Videosのリレーションが定義される
- [ ] Profile belongsTo User、Profile belongsTo thumbnail_video/popup_videoが定義される
- [ ] Video belongsTo Userが定義される
- [ ] $fillableまたは$guardedが適切に設定され、マスアサインメント対策が完了
- [ ] UserモデルにSoftDeletesトレイトが適用される

---

#### Issue #5: 認証機能実装（Laravel Breeze）

**概要**: Laravel Breezeの認証機能をカスタマイズし、ユーザー登録時にプロフィールレコードを自動作成

**依存**: #4

**タスク領域**: backend, frontend

**受け入れ基準(AC)**:
- [ ] ユーザー登録フォーム（/register）が動作し、バリデーションエラーが日本語で表示される
- [ ] ユーザー登録時にusersとprofilesテーブルに同時レコード作成される（トランザクション）
- [ ] ログインフォーム（/login）が動作し、認証成功時にダッシュボードへリダイレクト
- [ ] ログアウト機能（POST /logout）が動作する
- [ ] 未認証ユーザーが/dashboardにアクセスするとログインページへリダイレクト

---

#### Issue #6: トップページと公開プロフィールページの骨組み作成

**概要**: トップページ（/）と公開プロフィールページ（/users/:id）のBladeテンプレート作成

**依存**: #5

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] トップページ（/）が表示され、登録・ログインへのリンクが機能する
- [ ] PublicProfileController@showが実装され、/users/:idで404エラーまたはプロフィールページが表示される
- [ ] プロフィールページでユーザー名・経歴が表示される（動画は未実装でOK）
- [ ] 存在しないユーザーIDにアクセスすると404エラーページが表示される
- [ ] Tailwind CSSでレスポンシブデザインが適用される

---

### Phase 1: MVP Core

#### Issue #7: ダッシュボード実装

**概要**: 認証後のダッシュボード画面を実装し、プロフィール状態と各機能へのリンクを表示

**依存**: #6

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] DashboardController@indexが実装され、認証ユーザーのプロフィール情報を取得
- [ ] ダッシュボード画面にプロフィール完成度（名前・経歴・動画の有無）が表示される
- [ ] プロフィール編集/動画管理/プレビュー/公開ページへのリンクが配置される
- [ ] ログアウトボタンが機能する
- [ ] 管理者の場合のみ「ユーザー管理」リンクが表示される

---

#### Issue #8: プロフィール編集機能実装

**概要**: プロフィール編集フォームとバリデーション、更新処理を実装

**依存**: #7

**タスク領域**: backend, frontend

**受け入れ基準(AC)**:
- [ ] ProfileController@edit/updateが実装される
- [ ] UpdateProfileRequestでバリデーション（名前必須50文字以内、経歴1000文字以内）が動作
- [ ] プロフィール編集フォームで現在の情報が表示され、更新できる
- [ ] 動画選択ドロップダウンは後のIssueで実装するため、この段階では空でOK
- [ ] 更新成功時に「プロフィールを更新しました」メッセージが表示され、ダッシュボードへリダイレクト
- [ ] バリデーションエラーが日本語で表示される

---

#### Issue #9: プレビュー機能実装

**概要**: 自分の公開ページをプレビュー表示する機能を実装

**依存**: #8

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] PreviewController@showが実装され、認証ユーザーのプロフィールを取得
- [ ] プレビュー画面が公開プロフィールページと同じレイアウトで表示される
- [ ] 「これは下書き状態のプレビューです」という注意書きが表示される
- [ ] 編集ページへ戻るボタンが機能する
- [ ] 動画が未設定の場合はプレースホルダーが表示される

---

#### Issue #10: 公開プロフィールページの詳細実装

**概要**: 公開プロフィールページに経歴・動画表示を追加（動画は後のIssueで連携）

**依存**: #7

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] プロフィールページで経歴が改行を保持して表示される（whitespace-pre-wrap）
- [ ] 自己紹介欄がmin-heightで画面下まで表示される
- [ ] サムネイル動画エリア（円形、ボックス内右下配置）のプレースホルダーが表示される
- [ ] 「管理ページへ」リンクがボックス欄外に表示される
- [ ] ポップアップ動画のプレースホルダーが表示される（モーダル未実装でOK）
- [ ] Tailwind CSSでレスポンシブ対応される
- [ ] 動画が設定されていない場合は「動画未設定」と表示される

---

#### Issue #11: FormRequestクラス整備

**概要**: バリデーションロジックをFormRequestクラスに分離し、エラーメッセージを日本語化

**依存**: #8

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] UpdateProfileRequestクラスが作成され、バリデーションルールとメッセージが定義される
- [ ] RegisterRequest（Breeze既存）のエラーメッセージが日本語化される
- [ ] LoginRequest（Breeze既存）のエラーメッセージが日本語化される
- [ ] 全てのバリデーションエラーが設計書通りの日本語メッセージで表示される
- [ ] コントローラーでFormRequestが使用される

---

#### Issue #12: 動画一覧ページ実装（表示のみ）

**概要**: 動画一覧ページを実装し、自分の動画リストを表示（アップロード機能は次Issue）

**依存**: #9

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] VideoController@indexが実装され、認証ユーザーの動画リストを取得
- [ ] 動画一覧ページで動画ファイル名・ステータス（uploading/encoding/completed/failed）が表示される
- [ ] ステータスに応じたバッジ（青/黄/緑/赤）が表示される
- [ ] 「動画アップロード」ボタンが配置される
- [ ] 動画が0件の場合「まだ動画がありません」と表示される

---

#### Issue #13: 動画削除機能実装

**概要**: 動画削除処理を実装し、プロフィールで使用中の動画は削除不可とする

**依存**: #12

**タスク領域**: backend, frontend

**受け入れ基準(AC)**:
- [ ] VideoController@destroyが実装される
- [ ] 動画削除時に所有権チェック（他人の動画は削除不可）が実施される
- [ ] プロフィールで使用中（thumbnail_video_id/popup_video_id）の動画は削除不可
- [ ] 削除可能な動画はvideosテーブルから削除される（S3削除は後のIssue）
- [ ] 削除成功時に「動画を削除しました」メッセージが表示される
- [ ] 使用中の動画削除時に「この動画はプロフィールで使用中のため削除できません」エラーが表示される

---

#### Issue #14: プロフィール編集での動画選択機能追加

**概要**: プロフィール編集フォームに動画選択ドロップダウンを追加し、completed動画のみ選択可能にする

**依存**: #13

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] プロフィール編集フォームにサムネイル用動画/ポップアップ用動画の選択ドロップダウンが表示される
- [ ] ドロップダウンにはstatus='completed'の動画のみ表示される
- [ ] 動画が未選択の場合は「選択しない」オプションが表示される
- [ ] プロフィール更新時にthumbnail_video_id/popup_video_idが正しく保存される
- [ ] 選択された動画が自分の動画かどうかチェックされる

---

### Phase 2: 動画処理

#### Issue #15: S3連携実装（MinIO/本番S3）

**概要**: S3クライアント設定と動画アップロード・削除機能を実装

**依存**: #8

**タスク領域**: backend, infra

**受け入れ基準(AC)**:
- [ ] Laravel FilesystemでS3ドライバが設定される（ローカルはMinIO、本番はAWS S3）
- [ ] 動画アップロード時にS3へファイルがアップロードされる（パス: users/{user_id}/original/{video_id}.{ext}）
- [ ] 動画削除時にS3からファイルが削除される
- [ ] アップロード失敗時に適切なエラーハンドリングが実施される
- [ ] .envでS3設定（バケット名、リージョン、認証情報）が管理される

---

#### Issue #16: 動画アップロード機能実装

**概要**: 動画アップロードフォームとバリデーション、S3アップロード処理を実装

**依存**: #15

**タスク領域**: backend, frontend

**受け入れ基準(AC)**:
- [ ] VideoController@create/storeが実装される
- [ ] StoreVideoRequestでバリデーション（100MB以内、mp4/mov/avi/wmv形式）が動作
- [ ] 動画アップロード時にvideosテーブルにレコード作成（status: uploading）
- [ ] S3へ動画がアップロードされ、original_pathが保存される
- [ ] アップロード成功時にstatus='encoding'に更新される
- [ ] 動画の長さが60秒以内かチェックされる（getID3ライブラリまたはFFprobe使用）
- [ ] バリデーションエラーが日本語で表示される

---

#### Issue #17: 動画エンコード機能実装（FFmpeg同期処理）

**概要**: FFmpegを使用した動画エンコード処理を実装（Phase 1は同期処理）

**依存**: #16

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] VideoEncoderServiceクラスが作成される
- [ ] FFmpegで動画をmp4（H.264/AAC）にエンコードする処理が実装される
- [ ] エンコード成功時にencoded_pathへS3アップロードされる
- [ ] エンコード成功時にoriginal_pathがS3から削除される
- [ ] エンコード成功時にstatus='completed'に更新される
- [ ] エンコードタイムアウト（5分）が設定される
- [ ] エンコード中はユーザーに「エンコード中」と表示される

---

#### Issue #18: エンコードリトライ・エラーハンドリング実装

**概要**: エンコード失敗時のリトライロジック（最大3回）とエラーメッセージ保存を実装

**依存**: #17

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] エンコード失敗時にretry_countがインクリメントされる
- [ ] retry_count < 3の場合は自動的に再エンコードが実行される
- [ ] retry_count >= 3の場合はstatus='failed'に更新される
- [ ] エンコード失敗時にerror_messageにエラー内容が保存される
- [ ] エンコード失敗時にユーザーに「エンコード失敗」通知が表示される
- [ ] 失敗した動画は動画一覧ページで赤色のバッジが表示される

---

### Phase 3: 管理・運用

#### Issue #19: 管理者ミドルウェア実装

**概要**: 管理者権限チェックを行うミドルウェアを作成

**依存**: #6

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] AdminMiddlewareが作成される
- [ ] Auth::user()->role === 'admin'でチェックが実施される
- [ ] 管理者以外がアクセスすると403エラーページが表示される
- [ ] app/Http/Kernel.phpにミドルウェアが登録される
- [ ] routes/web.phpで/admin/*ルートにミドルウェアが適用される

---

#### Issue #20: 管理者ユーザー一覧ページ実装

**概要**: 管理者専用のユーザー一覧ページを実装

**依存**: #19

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] Admin\UserController@indexが実装される
- [ ] ユーザー一覧ページで全ユーザーのID/メール/名前/権限/作成日時が表示される
- [ ] 各ユーザーに編集・削除ボタンが配置される
- [ ] ページネーション（10件/ページ）が実装される
- [ ] 管理者以外がアクセスすると403エラーが表示される

---

#### Issue #21: 管理者ユーザー編集・削除機能実装

**概要**: 管理者が任意のユーザーのプロフィールを編集・削除できる機能を実装

**依存**: #20

**タスク領域**: backend, frontend

**受け入れ基準(AC)**:
- [ ] Admin\UserController@edit/update/destroyが実装される
- [ ] ユーザー編集フォームでプロフィール情報と権限（admin/user）を変更できる
- [ ] ユーザー削除時にソフトデリート（deleted_at）が実行される
- [ ] ユーザー削除時にプロフィールと動画がカスケード削除される
- [ ] ユーザー削除時にS3の動画ファイルも削除される
- [ ] 削除成功時に「ユーザーを削除しました」メッセージが表示される

---

#### Issue #22: CloudWatch監視設定（本番環境）

**概要**: CloudWatchでEC2/RDSのログ・メトリクス監視を設定

**依存**: #1（インフラ構築完了後）

**タスク領域**: infra

**受け入れ基準(AC)**:
- [ ] EC2インスタンスのCPU/メモリ使用率がCloudWatchで監視される
- [ ] RDSの接続数・ストレージ使用率がCloudWatchで監視される
- [ ] Laravelのログ（storage/logs/laravel.log）がCloudWatch Logsに送信される
- [ ] エラーログが発生した際にアラート通知が設定される（オプション）
- [ ] CloudWatch Dashboardで主要メトリクスが可視化される

---

### Phase 4: 堅牢化

#### Issue #23: 単体テスト実装（Model/Service）

**概要**: Eloquentモデルとサービスクラスの単体テストを作成

**依存**: #14, #18

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] User/Profile/Videoモデルのリレーションテストが作成される
- [ ] VideoEncoderServiceのエンコードロジックテストが作成される（モック使用）
- [ ] バリデーションルールのテストが作成される
- [ ] `php artisan test`で全テストが成功する
- [ ] テストカバレッジが主要ロジックで80%以上

---

#### Issue #24: 機能テスト実装（Controller/Route）

**概要**: コントローラーとルートの機能テストを作成

**依存**: #23

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] 認証機能（登録/ログイン/ログアウト）のテストが作成される
- [ ] プロフィール編集機能のテストが作成される
- [ ] 動画アップロード・削除機能のテストが作成される
- [ ] 管理者機能（ユーザー一覧/編集/削除）のテストが作成される
- [ ] 権限チェック（未認証/一般ユーザー/管理者）のテストが作成される
- [ ] `php artisan test`で全テストが成功する

---

#### Issue #25: セキュリティ強化（XSS/CSRF/SQLi対策）

**概要**: セキュリティ脆弱性をチェックし、XSS/CSRF/SQLインジェクション対策を強化

**依存**: #24

**タスク領域**: backend, frontend

**受け入れ基準(AC)**:
- [ ] BladeテンプレートでXSS対策（{{ }}でエスケープ）が徹底される
- [ ] 全てのフォームに@csrfディレクティブが含まれる
- [ ] Eloquent ORMまたはクエリビルダのみ使用し、生SQLが存在しない
- [ ] ファイルアップロード時のMIMEタイプチェックが実施される
- [ ] S3バケットのアクセス権限が適切に設定される（パブリック読み取りのみ）
- [ ] パスワードがbcryptでハッシュ化される

---

#### Issue #26: パフォーマンス最適化（N+1問題/キャッシュ）

**概要**: N+1問題を解消し、頻繁にアクセスするデータをキャッシュ

**依存**: #25

**タスク領域**: backend

**受け入れ基準(AC)**:
- [ ] 公開プロフィールページでN+1問題が発生しない（Eager Loading使用）
- [ ] 動画一覧ページでN+1問題が発生しない
- [ ] ダッシュボードでプロフィール情報をキャッシュ（Cache::remember使用）
- [ ] Laravel Debugbarで不要なクエリが検出されない
- [ ] ページ読み込み時間が3秒以内（設計書要件）

---

### Phase 5: 動画表示

#### Issue #27: 動画表示機能実装

**概要**: 公開プロフィールページおよびプレビューページで、実際に動画を再生表示する機能を実装。サムネイル動画（自動再生・ループ・ミュート）とポップアップ動画（モーダル表示・自動再生・音声あり）を実装。

**依存**: #10, #14

**タスク領域**: frontend, backend

**受け入れ基準(AC)**:
- [ ] サムネイル動画が円形でボックス内右下に表示され、自動再生・ループ・ミュートで再生される
- [ ] サムネイルクリックでポップアップ動画がモーダル表示される
- [ ] ポップアップ動画はモーダル表示時に自動再生、音声あり、コントロール（再生/一時停止、音量、シークバー）が表示される
- [ ] オーバーレイクリック、×ボタン、Escapeキーでモーダルが閉じる
- [ ] 動画未設定時はプレースホルダーが表示される
- [ ] status='completed'の動画のみ表示される
- [ ] レスポンシブ対応（モバイル・タブレット・デスクトップ）
- [ ] プレビューページでも同じ動画表示機能が動作する

---

### Phase 6: DB統一

#### Issue #28: PostgreSQL への移行（MySQL 廃止）

**概要**: Laravel 版が PostgreSQL 単一 DB 構成に統合されたのに合わせ、Node.js 版も MySQL → PostgreSQL へ移行。Prisma の `provider` を `postgresql` に変更し、初期マイグレーションをリセットして再生成する。チャットボットは今回導入しない。

**依存**: #3

**タスク領域**: backend, infrastructure, database

**受け入れ基準(AC)**:
- [x] `schema.prisma` の `provider` が `postgresql` である
- [x] `migration_lock.toml` の `provider` が `postgresql` である
- [x] `docker-compose.yml` の DB サービスが `postgres:16` で `pg_isready` ヘルスチェック付き
- [x] `.env.example` / `.env.production.example` / `.env.test` の `DATABASE_URL` が `postgresql://` 形式
- [x] `deploy/deploy.sh` の URL 例が PostgreSQL 形式
- [ ] ローカルで `docker compose down -v && docker compose up -d db && prisma migrate dev --name init` が成功する
- [ ] `npm run test:e2e` が全件パスする
- [ ] 手動検証: 登録 → プロフィール作成 → 動画アップロード → エンコード → 公開プロフィール閲覧が一貫動作する

---

## 確認事項

### インフラ関連
- **CloudFormation実装**: Issue一覧に含めるか？（Phase 0で手動構築、Phase 2で自動化など）
- **本番デプロイ手順**: CI/CDパイプライン（GitHub Actions等）は必要か？

### 動画処理関連
- **FFmpegインストール**: Issue #1（Docker環境構築）に含めるか、別Issueとするか？
- **エンコードキュー**: Phase 1は同期処理だが、キュー実装（Laravel Queue）を後のPhaseで追加するか？

### テスト関連
- **E2Eテスト**: Dusk/Cypressなどのブラウザテストは必要か？

### 運用関連
- **バックアップ設定**: RDSの自動バックアップ設定Issueを追加するか？（Phase 2機能だが必要性高い）
- **ソフトデリートのクリーンアップ**: 30日後の物理削除バッチ処理Issueを追加するか？
